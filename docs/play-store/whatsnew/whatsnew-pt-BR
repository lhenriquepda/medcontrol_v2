v0.2.6.9 — hotfix lentidão pós minutos

• App não fica mais lento depois de alguns minutos aberto — removida instrumentação verbose do caminho crítico que sobrecarregava o WebView.
• Reconexão automática: checagem ativa de saúde a cada 60s + reconnect WebSocket se necessário (antes só rodava após 5min de tela bloqueada).
• Dashboard não refaz a busca pesada em cada foco — atualização vem via realtime + puxar pra atualizar.
