# 2026-05-17 — Release v0.2.3.10 SHIPPED

**Tag:** `v0.2.3.10` · master commit `7c42b15`
**Play Console:** Internal Testing publicado 15:28 BRT · vc 73 vn 0.2.3.10
**Vercel prod:** dosymed.app v0.2.3.10 confirmado 18:30 UTC

## Entregue (3 itens)

| Item | Tipo | Fix |
|---|---|---|
| **#295** | P2 BUG | `listDoses` JOIN inline `patients(name)` + `useDashboardPayload.dosesComputed` enriquece com payload.patients. Alarme owner não mostra mais "Sem paciente". |
| **#296** | P2 BUG | `Dashboard.handleRefresh` invalida todos namespaces (`dashboard-payload`, `doses`, `patients`, `treatments`, `received-shares`). Pull-to-refresh limpa fantasmas pós-edição em outro device. |
| **#297** | P1 BUG LGPD | Edge `patient-unshare-handler` v1 ACTIVE + DB trigger `trg_notify_patient_share_deleted` ON `patient_shares` DELETE + Java `handlePatientUnshared` + MainActivity propaga via JS event `dosy:patientUnshared` → App.jsx listener invalida cache + remove paciente fantasma. |

## Edge Functions / Migrations

| Function | Versão |
|---|---|
| patient-unshare-handler | v1 ACTIVE |

| Migration | Propósito |
|---|---|
| `20260517170000_patient_unshare_notification_trigger_v0_2_3_10.sql` | Trigger DELETE patient_shares → pg_net.http_post |

## QA emulador #297

Caregiver background recebeu FCM patient_unshared:
- Java: `patient_unshared dispatched patientId=5da7d6fd...`
- MainActivity: `[handleAlarmAction] extras: unsharePatientId=5da7d6fd...`
- JS listener `dosy:patientUnshared` executou cache cleanup

## Pendências device físico Samsung S25 Ultra

- [ ] Alarme NÃO mostra "Sem paciente" (criar paciente + dose +1min)
- [ ] Pull-to-refresh Dashboard remove paciente compartilhado fantasma após owner revogar share
- [ ] Unshare em background app killed: ao abrir, paciente já não está visível
