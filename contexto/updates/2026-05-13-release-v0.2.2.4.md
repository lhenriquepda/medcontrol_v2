# Release v0.2.2.4 — 2026-05-13

**Tipo:** P2 CLEANUP (dose_alarms_scheduled tabela órfã)

**versionCode:** 62
**versionName:** 0.2.2.4
**Tag git:** `v0.2.2.4`
**AAB Internal Testing:** ⚠️ pendente (validado via Dosy-Dev Studio Run + monitor logcat)

## Motivação

Auditoria pós-#213 identificou tabela `medcontrol.dose_alarms_scheduled` órfã:
- Criada #083.7 (v0.1.7.2) pra crons `notify-doses-1min` + `schedule-alarms-fcm-6h` skiparem push se alarme local agendado
- Ambos crons UNSCHEDULED em #209
- 2 writers ativos (JS scheduler + Java FCM handler) gerando ~13k upserts/dia/device
- 0 readers ativos

`alarm_audit_log` v0.2.2.0 substitui rastreio completamente.

## Implementação

### `src/services/notifications/scheduler.js`

Removido bloco upsert + imports unused:
```diff
-import { supabase, hasSupabase } from '../supabase'
-import { ..., getDeviceId } from '../criticalAlarm'
+import { scheduleCriticalAlarmGroup, isCriticalAlarmAvailable, checkCriticalAlarmEnabled } from '../criticalAlarm'

-        // Item #083.7 — reporta dose_alarms_scheduled pra cada dose
-        try {
-          const deviceId = await getDeviceId()
-          if (deviceId && hasSupabase) {
-            const rows = group.map(dose => ({ doseId, userId, deviceId, via: 'app-foreground' }))
-            const { error } = await supabase.from('dose_alarms_scheduled').upsert(rows, ...)
-            if (error) console.warn('[Notif] dose_alarms_scheduled upsert:', error.message)
-          }
-        } catch (e) {
-          console.warn('[Notif] report alarm scheduled fail:', e?.message)
-        }
+        // v0.2.2.4 (#214) — REMOVIDO dose_alarms_scheduled upsert.
+        // alarm_audit_log v0.2.2.0 substitui rastreio.
```

### `android/.../DosyMessagingService.java`

Removido método `reportAlarmScheduled()` (~50 linhas) + call sites em `handleScheduleAlarms` + imports HTTP unused (`BufferedReader`, `IOException`, `InputStreamReader`, `OutputStream`, `HttpURLConnection`, `URL`, `StandardCharsets`).

### DB Migration `drop_dose_alarms_scheduled_v0_2_2_4`

```sql
DROP TABLE IF EXISTS medcontrol.dose_alarms_scheduled CASCADE;
```

## Validação device vc 62 (Dosy-Dev Studio Run)

**Boot burst:**
```
16:17:22 batch #1 initial (dosesCount=436, 58 scheduled)
16:18:01 batch #2 (+39s trailing)
16:18:32 batch #3 (+30s trailing)
16:28:42 batch #4 (+10min — drain)
```

**Mark dose tomada (N-acetilcisteína):**
```
19:30:46 js_scheduler batch (57 scheduled — 1 dose fora)
```

**Mark dose pulada (Citrato de Magnésio):**
```
19:31:15 js_scheduler batch (56 scheduled — 2 doses fora)
```

**Undo 1ª (Citrato pulada → pending):**
```
19:32:26 js_scheduler batch (57 scheduled — voltou)
19:32:29 edge_trigger_handler fcm_sent triggerReason=undo_to_pending
19:32:28 java_fcm_received batch_start/scheduled/batch_end
```

**Undo 2ª (N-acetilcisteína tomada → pending):**
```
19:32:56 js_scheduler batch (58 scheduled — estado original ✅)
19:32:30 edge_trigger_handler fcm_sent triggerReason=undo_to_pending
19:32:30 java_fcm_received batch_start/scheduled/batch_end
```

**Fluxo E2E confirmado em 4 caminhos paralelos:**
1. JS App.jsx signature → rescheduleAll
2. DB trigger `notify_dose_change` → http_post pg_net
3. Edge `dose-trigger-handler` v17 → FCM data
4. `DosyMessagingService.handleScheduleAlarms` → `AlarmScheduler.scheduleDose` nativo

**Zero referências `dose_alarms_scheduled` no logcat OR DB writes** ✅ #214 cleanup confirmado.

## Egress estimado

| Componente | Antes (v0.2.2.3) | Após (v0.2.2.4) | Redução |
|---|---|---|---|
| `dose_alarms_scheduled` upsert/dia | ~50 | 0 | -100% |
| HTTP requests/dia | ~50 | 0 | -100% |
| **Total egress MB/dia/device** | **~1-2** | **~0.5-1** | -50% incremental |

## Commits chave

- `1055e39` chore(perf): #214 P2 CLEANUP — remove dose_alarms_scheduled órfã (v0.2.2.4 vc 62)
- `83abfb1` Merge release/v0.2.2.4 — #214 P2 CLEANUP dose_alarms_scheduled órfã (master)

## Pendente

- AAB vc 62 build Studio release variant + upload Play Console Internal Testing
