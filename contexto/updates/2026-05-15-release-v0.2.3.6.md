# 2026-05-15 — Release v0.2.3.6 SHIPPED

**Tag:** `v0.2.3.6` · master commit `348eff7`
**Play Console:** Internal Testing publicado 21:55 BRT · vc 69 vn 0.2.3.6
**Vercel prod:** auto-deploy via master push

## Entregue

### Features
- **#250** ANVISA autocomplete medicamentos — `medications_catalog` 764 rows + `search_medications` RPC unaccent + `MedNameInput` 3-fontes (local + ANVISA + userMeds) + `useMedCatalogSearch` debounce 300ms

### Bug fixes P1
- **#255** idle longo (>1h token expirado) → skeleton infinito — fix v2 `inactiveMs > SUPABASE_TOKEN_LIFETIME_MS` em `useAppResume`
- **#256b** SOS submit trava silencioso (window.confirm bug Capacitor + onClose toast + `p_force` param RPC `register_sos_dose`)
- **#257b** `processLock` + `lockAcquireTimeout: 15s` em `supabase.js`
- **#258b** sharing Dashboard via `get_dashboard_payload` CTE (cache stale + auth lock + RPC ignorando shares)
- **#264** dose 1ª passada não pulada em `create_treatment_with_doses` — SQL removeu WHILE pula-passado, doses pendentes anteriores ficam visíveis como "atrasada" via recompute
- **#266** PatientDetail não mostrava tratamento recém-criado — `insertEntityIntoLists` em todas variações `['treatments', *]`
- **#267** Dashboard skeleton em hour boundary — `placeholderData` fallback cross-key
- **#268** supabase-js travado pós-idle — `getSession()` validation timeout 5s em `useAppResume`
- **#269** stale cache overdue badge — `useDoses(overdueFilter, {pollIntervalMs:60_000})` no AppHeader

### Bug fixes P2-P4
- **#253b** email template wordmark texto (img→CSS)
- **#254b** self-patient signup via user_metadata + session mutex + cache invalidate
- **#259** "Cancelada" persiste em Reports pós pause/resume — fix SQL `update_treatment_schedule` detecta resume + DROP overload antigo + DELETE inclui cancelled
- **#260** console errors `[object Object]` — serializar via `err?.message`
- **#261** HORÁRIO SOS en-US — split em `type="date"` + `type="time"`
- **#262** ad banner posição — TOP_CENTER (revertido após tentativa BOTTOM)
- **#263** "1 dia" singular + "Termina hoje/amanhã" relative em TreatmentList
- **#265** total doses count exato `CEIL(durationDays × 24 / intervalHours)`
- **#270** banner "Sincronizando..." quando isFetching + stale 8-60s
- **#271** invalidateQueries `['dashboard-payload']` em todas mutations (create/update/delete Treatment + Patient + registerSos)

## Validação
- **Chrome MCP localhost** teste-plus@teste.com: todos fluxos UI
- **Emulator Pixel8_Test APK debug** teste-plus: idle 2× 25min cross hour boundary OK
- **DB consistency**: badge sino ↔ hero ↔ mini stat ↔ lista doses ↔ DB count

## Pendente device físico user
- Idle >1h token genuinamente expirado → redireciona login
- ANVISA autocomplete device
- Sharing Daffiny/Luiz cross-device
- Push resumo diário cron 5am BRT
- AdMob banner real ad units PROD

## Pós-release tasks
- [x] Tag git `v0.2.3.6` push origin
- [x] Merge `release/v0.2.3.6` → master linear
- [x] Play Console Internal Testing publish
- [x] Vercel auto-deploy via master push
- [ ] Validação device físico S25 Ultra (user)
- [ ] Re-ler README §1-5 pra próxima session

## Commits principais
```
0436e01 docs(v0.2.3.6): release notes pt-BR whatsnew
66d0cd7 fix(v0.2.3.6 #268 #269 #270 #271 + revert #262): bugs cascata
55e7ae9 docs(v0.2.3.6): Validar.md idle 53min emulator validado
b3f8baa fix(v0.2.3.6 #259 #260 #261 #262 #263): segunda batelada QA bugs
ff3e0bd docs(v0.2.3.6): sync 4 docs (Passo 9)
20efdbf fix(v0.2.3.6 #267): Dashboard skeleton hour boundary
4113639 fix(v0.2.3.6 #264 #265 #266): treatment create
6ac556e fix(v0.2.3.6 #255b): idle fix correto
085c5f6 fix(v0.2.3.6): SOS force param + ConfirmDialog
6c08185 fix(v0.2.3.6): lockAcquireTimeout 15s
2380594 fix(v0.2.3.6): SOS submit trava silencioso
af3d14b fix(v0.2.3.6): sharing system broken
1418f1a feat(v0.2.3.6 #250b): busca medicamentos acent-insensitive
222fd59 feat(v0.2.3.6 #250): autocomplete ANVISA
```
