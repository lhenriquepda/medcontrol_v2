# Release v0.1.6.10 — 2026-05-01

> **Sessão:** ~3h · **Agente:** Claude Opus 4.7 · **Versão:** 0.1.6.9 → **0.1.6.10** (versionCode 22→23)
> **Tag git:** `v0.1.6.10` · **Tipo de release:** patch (security + data + process)

---

## 🎯 Objetivo da sessão

Continuar do ROADMAP §4 e fechar P0 com código. Originalmente #001 send-test-push admin auth. Acabou cobrindo 3 itens P0 + reorganização contexto/ + 2 mudanças de processo (Regra 8 comunicação não-dev, modelo "1 sessão = 1 release branch").

## ✅ O que foi feito

### Items ROADMAP fechados

- **#001 — Admin auth check em `send-test-push` Edge Function** (P0, [Auditoria] BUG-002)
  - Bearer JWT obrigatório → 401 sem header / inválido
  - Lookup `medcontrol.admins.user_id = auth.uid()` → 403 non-admin
  - Validado em prod via curl em 6 cenários (401×4, 405×1, 403×1)
- **#002 — Sanitizar email enumeration em `send-test-push`** (P0, [Auditoria] BUG-015)
  - Resposta neutra `{ ok: true, sent: 0 }` em todos fluxos sem entrega
  - Removido echo email + per-token results no body
- **#005 — BUG-001 encoding UTF-8 paciente** (P0, [Auditoria])
  - Diagnóstico: dado seed legacy "Jo�o Teste" com bytes `ef bf bd` (U+FFFD literal); DB e PostgREST round-trip íntegros
  - Cleanup: row deletada via REST service_role
  - Verificação: insert via REST como teste03 com "João da Silva ÃÕÉÍÇãõéíç" → bytes utf-8 idênticos in/out

### Process improvements

- **Reorganização `contexto/`** (commit `d53266b`):
  - 5 docs raiz legados (`Plan.md` 62KB, `RoadMap.md`, `SECURITY.md`, `Contexto.md`, `Plan_Suggestions.md`) → `contexto/archive/`
  - `auditoria/` snapshot 2026-05-01 imutável
  - Templates `decisoes/README.md` + `updates/README.md`
  - `README.md` canônico + `PROJETO.md` + `ROADMAP.md` + `CHECKLIST.md`
- **Regra 8 — Comunicação user não-dev** (commit `6ec13a6`, autoria user):
  - Vocabulário proibido ao falar com user (JWT, branch, commit, deploy etc → traduções)
  - Templates: "Decisão de aprovação", "Reporte de teste", "Pergunta sobre coisas técnicas"
  - Auto-checagem antes de enviar resposta
- **Modelo "1 sessão = 1 release branch"** (commits `b854eb0` + `826cd10`):
  - Branch `release/v{X.Y.Z}` única por sessão; todas mudanças nela
  - Ciclo de release dos 8 passos no fim (bump → AAB → Console → merge → tag → Vercel → contexto → cleanup)
  - Hotfix scenario documentado pra exceções
  - 11 lugares do README alinhados com novo modelo
- **README audit + 10 fixes pós-modelo** (commit pendente):
  - §"Estado atual" atualizado v0.1.6.10 (era 0.1.6.9 desatualizado)
  - Edge case "só docs vai direto master" removido (contradizia modelo)
  - §Início rápido item 4 — leia último log (era "1-2 logs")
  - §Pre-trabalho passo 2 — comando robusto (`--list 'release/*'`)
  - Edge Function deploy/rollback documentado em §Decisão onde testar
  - §Princípios passo 1 release — release = 3 ambientes sincronizados (era só git)
  - Bloco §Worktrees removido (órfão, decidido não usar)
  - Regra ouro 3ª coisa — "quando publica?" (era "descartar branch?")
  - §Branch/PR marcada como FONTE CANÔNICA do modelo
  - Vocabulário proibido escopado a "AO FALAR COM USER" (Regra 8)

### Deploys executados

- **Edge Function `send-test-push`** deployada em `guefraaqbkcehofchnrc` (Supabase prod) via `supabase functions deploy` + PAT — antes do merge final pra validar via curl
- **Patient legacy `46d9196f`** deletado em prod via REST service_role
- **Tag git `v0.1.6.10`** + push origin
- **Vercel produção** auto-deploy a partir do push master (~2min)
- **Play Store Internal Testing** AAB v0.1.6.10 (versionCode 23) — uploaded via Console

## 📦 Items do ROADMAP fechados

- [x] **#001** Admin auth check em `send-test-push`
- [x] **#002** Sanitizar email enumeration
- [x] **#005** Encoding UTF-8 paciente legacy

## 🐛 Bugs novos descobertos

Nenhum. CHECKLIST §#001 snippet original tinha typo (`userId` em vez de `user_id`); corrigido inline.

## 🧠 Decisões tomadas

- **Modelo "1 sessão = 1 release branch":** decidido durante a sessão em resposta a confusão do user com fluxo "branch por mudança". Versão final: branch única `release/v{X.Y.Z}` acumula todos commits da sessão; merge + AAB + Console + tag no fim.
- **Não criar ADR formal:** decisão de processo (não arquitetural). Documentada na Regra 8 + §Branch do README.
- **Edge Function deploy durante sessão:** deploy server-side antes do merge final aceito como prática (rollback simples = git checkout tag-anterior + redeploy).
- **Não testar caminho admin (200) em send-test-push:** decisão pragmática — 6 testes cobrem auth gate + admin gate; admin path = mesma lógica antiga + check inverso. Risco baixo.

## 📁 Arquivos da pasta `contexto/` atualizados

- `README.md` — Regra 8 + modelo branch + 10 fixes audit (~+200 linhas)
- `PROJETO.md` — versão 0.1.6.10, versionCode 23, build.gradle path
- `ROADMAP.md` — §1 versão atual, §3 onde paramos (v0.1.6.10 + items fechados), §4 P0 restantes manuais, §6 [x] em #001/#002/#005, §12 contadores P0 9→6
- `CHECKLIST.md` — Status #001/#002/#005 → ✅ Concluído + corrigido `userId`→`user_id` snippet
- `updates/2026-05-01-fix-send-test-push-auth.md` — log fix #001+#002 (intermediário)
- `updates/2026-05-01-fix-encoding-utf8-pacientes.md` — log fix #005 (intermediário) + recipe Python pra reauditoria
- `updates/2026-05-01-release-v0.1.6.10.md` — este arquivo (consolida sessão)

## 🚧 Estado deixado pra próxima sessão

- **Branch release deletada** após merge.
- **Master** = `tag v0.1.6.10` = Vercel produção = Play Store AAB v0.1.6.10.
- **3 ambientes sincronizados.**
- **Próxima sessão:** ROADMAP §4 P0 restantes são manuais user (#003 #004 #006 #007 #008 #009). Próxima sessão de código sugerida = `release/v0.1.7.0` minor com batch P1: #010 #011 #012 #013 #014 #019 #022 #023 #024.
- **Bloqueadores externos esperando user:**
  - #003 rotação senha postgres (~30min você)
  - #008 Sentry GitHub Secrets (~15min você)
  - #004 vídeo FGS YouTube (~2-3h você)
  - #006 device validation 3 devices (1-2 dias você)
  - Upgrade Supabase Pro plan (custo) pra desbloquear #009

## 💬 Notas livres

- **Sessão híbrida:** primeiros commits foram em master direto (docs reorg) + 2 branches paralelas (security/* e fix/*) antes do user pedir modelo unificado. A partir desse momento, tudo consolidado em `release/v0.1.6.10`. Modelo limpo aplicado de v0.1.7.0 em diante.
- **AAB build executado pelo user em Android Studio** (Build → Generate Signed Bundle / APK → Android App Bundle, keystore `dosy-release.keystore`, variant release). Tamanho esperado ~17-20 MB.
- **Console upload via Claude in Chrome** — tab criada navegando `play.google.com/console`; user fez drag-drop AAB; release notes preenchidos colaborativamente.
- **Single-dev sem CI completa** — gates manuais antes de commit (lint, test). Pre-commit hooks ainda pendentes (#024 P1).

## 📊 Métricas

- Commits no master desta sessão: ~10
- LOC tocadas: +~600 docs / +~100 código (Edge Function)
- Branches criadas e mergeadas: 2 → consolidadas em 1 (`release/v0.1.6.10`)
- Tempo de sessão: ~3h
- Testes em prod via curl: 6 cenários (todos passaram)
- Pacientes deletados em prod: 1 (`46d9196f` legacy seed teste03)

## 🔜 Validação 24-48h pós-release

- Sentry: crash spike?
- Internal Testing testers: comportamento alarme + push?
- Play Console: ANR rate?
- PostHog: queda de eventos? (depende #015 PostHog key configurada)
