# Release v0.2.2.2 — 2026-05-13

**Tipo:** P1 STORM ROOT CAUSE (watchdog + signature guard)

**versionCode:** 60
**versionName:** 0.2.2.2
**Tag git:** `v0.2.2.2`
**AAB Internal Testing:** publicado 2026-05-13 15:14 BRT

## Motivação

Audit polling 11min pós-deploy v0.2.2.1 (hotfix throttle) confirmou storm continuou:
- 15 batches em 11min = 1.36 batches/min
- Esperado plan #209: ~10/dia (cron 5h + Worker 6h + interações reais)
- Realidade: ~2000/dia. **200× excesso**

Throttle reduziu impacto mas não eliminou root cause. Single trailing run continuava a cada 60s.

## Egress estimado pré-fix

| Componente | Egress/dia/device |
|---|---|
| `useDoses` refetch (14 dias window) | ~17 MB |
| `dose_alarms_scheduled` upsert | ~5-10 MB |
| Realtime channel reconnect | ~7 MB |
| `alarm_audit_log` INSERT | ~1 MB |
| **Total** | **~30-40 MB/dia/device** |

## Root cause (cadeia 60s)

1. `useRealtime.js` mantém WebSocket canal `realtime:userId:gen:ts`
2. `WATCHDOG_INTERVAL_MS = 60_000` — vigia roda a cada 60s checando `channel.state`
3. Em Android Doze + token JWT refresh window, state muda pra `!== 'joined'` ocasionalmente
4. Watchdog dispara `unsubscribe + subscribe + refetchQueries({queryKey: ['doses'], type: 'active'})`
5. Refetch retorna fresh data — mesmo conteúdo, mas array ref novo (timestamps microsec diferem)
6. `App.jsx useEffect` dep `allDoses` detecta "mudou" → `scheduleDoses(allDoses, {patients})` → `rescheduleAll`
7. Throttle v0.2.2.1 previne concurrent execution mas trailing run sempre dispara → storm 60s+30s cycles

## Implementação

### Fix 1: Watchdog interval 60s → 300s (5min)

`src/hooks/useRealtime.js`:
```js
const WATCHDOG_INTERVAL_MS = 300_000 // 5min (era 60_000)
```

Reduz frequência reconnect cycle 5×. Status callbacks supabase-js (`CLOSED/CHANNEL_ERROR/TIMED_OUT`) continuam funcionando — watchdog é safety net only.

### Fix 2: Signature guard em useEffect

`src/App.jsx`:
```js
const dosesSignature = useMemo(() => {
  if (!dosesLoaded) return ''
  return (allDoses || [])
    .map(d => `${d.id}:${d.status}:${d.scheduledAt}`)
    .sort()
    .join('|')
}, [allDoses, dosesLoaded])

const patientsSignature = useMemo(() => {
  if (!patientsLoaded) return ''
  return (allPatients || [])
    .map(p => `${p.id}:${p.name || ''}`)
    .sort()
    .join('|')
}, [allPatients, patientsLoaded])

useEffect(() => {
  if (!user || !dosesLoaded || !patientsLoaded) return
  scheduleDoses(allDoses, { patients: allPatients })
}, [user, dosesSignature, patientsSignature, dosesLoaded, patientsLoaded, scheduleDoses])
```

Refetch retorna mesma signature → useEffect não re-fires. Mudança real (status pending→done, scheduledAt change, dose new) → signature flipa → reschedule.

## Cleanup

`TRUNCATE medcontrol.alarm_audit_log` antes vc 60 install — baseline limpo pra validação.

## Critério de aceitação

- ✅ Build verde + AAB vc 60 publicado Internal Testing
- 🧪 Device runtime vc 60: app aberto 10min → ≤2 batches no audit
- 🧪 60min uso normal: ≤10 batches total
- 🧪 Idle 30min: ≤2 batches
- 🧪 Marcar dose como tomada → 1 batch_start aparece (signature flipou)

## Commits chave

- `f0fb05a` fix(perf): #212 P1 STORM ROOT CAUSE — watchdog 300s + signature guard (v0.2.2.2 vc 60)

## Não-escopo (futuro)

- Investigar pq `channel.state !== 'joined'` frequente em S25 Ultra (Android Doze suspending WS, token refresh churn) — item #213
- Telemetria PostHog `storm_detected` (rows/min > threshold alert)
