# release/v0.2.3.0 — Refactor Alarme + Push 3-Cenários — 2026-05-13

> **Sessão:** ~19:20 – 20:15 BRT (continuação auditoria 16:30) · **Agente:** Claude Opus 4.7 · **Versão app:** v0.2.2.4 → **v0.2.3.0** (vc 62→63)

---

## 🎯 Objetivo da sessão

Implementar refactor completo do sistema Alarme + Push baseado em:
- Auditoria 2026-05-13 (`contexto/auditoria/2026-05-13-alarme-push-auditoria.md`)
- Plano 3 cenários proposto pelo user
- 11 decisões consolidadas em CHECKLIST §#215

User autorizou: "Faça merge docs/auditoria-alarme-push → master + abre release/v0.2.3.0 + se necessário ataca #216-#221 pós refactor."

## ✅ O que foi feito

### Merge `docs/auditoria-alarme-push` → master

- Commit merge linear `db89a8f` (no-ff). Pushed `origin/master`.
- Vercel auto-deploy trigger.
- Updates log `2026-05-13-auditoria-alarme-push.md` + ROADMAP §3 sync (commit `bee726c`).

### Release `release/v0.2.3.0` aberta + bump versão

- Commit `21f8f32` — bump versionCode 62→63 + versionName 0.2.3.0 + VERSION_CODE_TO_NAME map +63 entry.

### Etapa 1 — Helper unificado JS + Java (commit `e45d1d5`)

**JS-side:**
- `src/services/notifications/unifiedScheduler.js` (NOVO 165 linhas)
  - `decideBranch(dose, prefs)` → 3 branches: push_critical_off | push_dnd | alarm_plus_push
  - `computeHorizon(doses, prefs)` → 24h se projetados > 400, senão 48h (decisão 8)
  - `buildSchedulePayload(group, prefs)` → alarmPayload + trayNotifPayload + metadata
  - `BACKUP_OFFSET = 700_000_000`
  - `DND_VIBRATION_PATTERN = [0, 200]` (decisão 3)
- `src/services/notifications/channels.js`
  - Novos `TRAY_CHANNEL_ID = 'dosy_tray'` + `TRAY_DND_CHANNEL_ID = 'dosy_tray_dnd'`
  - `dosy_tray_dnd`: IMPORTANCE_DEFAULT + sound null + vibração leve
- `src/services/criticalAlarm.js`: nova função `syncUserPrefs` (criticalAlarm + DnD)
- `src/hooks/useUserPrefs.js`: chama syncUserPrefs no load + em mudanças

**Java-side:**
- `AlarmScheduler.java`
  - `idFromString` agora aplica `% 2147483647` (alinha JS, #220 incluído)
  - Helper `scheduleDoseAlarm(ctx, groupId, triggerAt, doses)` lê prefs SharedPreferences `dosy_user_prefs` + decide branch + agenda alarme + tray
  - `isInDndWindow(epochMs, dndStart, dndEnd)` America/Sao_Paulo + cruza meia-noite
  - `cancelDoseAlarmAndBackup(ctx, groupId)` cancela alarme + tray backup
  - `scheduleTrayNotification` via `AlarmManager.setExactAndAllowWhileIdle` + TrayNotificationReceiver
  - `ensureTrayChannel` cria dosy_tray / dosy_tray_dnd Android-side
- `TrayNotificationReceiver.java` (NOVO 102 linhas) — dispara LocalNotification tray
- `AlarmReceiver.java` cancela LocalNotification backup antes startForegroundService (anti-duplicate)
- `CriticalAlarmPlugin.java` novo método `syncUserPrefs`
- `MainActivity.cleanupLegacyChannels()` remove doses_v2 + doses_critical_v2

### Etapa 2 — Cenário 01 refactor (commit `04bbbef`)

- `src/services/notifications/scheduler.js` `rescheduleAll` delega ao unifiedScheduler
- Janela dinâmica via `computeHorizon` aplicada
- Audit log enriquecido `{branch, horizon, source_scenario, criticalAlarmEnabled, dndEnabled, inDndWindow, projectedItems}`
- Throttle 30s + signature guard pós #211/#212 preservados
- Branch alarm_plus_push degrada gracefully pra push_critical_off se permission negada

### Etapa 3 — Cenário 02 server-side (commit `a2eb69c`)

- **#217 incluído** — `supabase/functions/daily-alarm-sync/index.ts` + `_shared/auditLog.ts` commitados local (fix drift repo↔prod)
- **#221 incluído** — `supabase/functions/dose-trigger-handler/index.ts` expand:
  - DELETE → action=cancel_alarms cross-device (cuidadores via patient_shares)
  - UPDATE pending→non-pending → action=cancel_alarms cross-device
  - Horizon 6h → 48h (B-09 fix)
  - getRecipientUserIds resolve owner + cuidadores (decisão 6 + 10)
- **Edge deploy via MCP:**
  - `dose-trigger-handler` v18 ACTIVE
  - `daily-alarm-sync` v3 ACTIVE — chunking 30 doses/FCM (#225 incluído) + janela dinâmica
- **Migration applied via MCP:** `expand_dose_change_notify_to_delete_v0_2_3_0`
  - Trigger AFTER INSERT/UPDATE/DELETE com OLD record em UPDATE/DELETE
  - Edge decide schedule/cancel baseado em record + old_record

### Etapa 4 — Java helper paridade (Cenário 03)

- `DoseSyncWorker.java` chama `AlarmScheduler.scheduleDoseAlarm` (substitui scheduleDose raw)
- `DosyMessagingService.java` handleScheduleAlarms idem
- `handleCancelAlarms` agora chama `AlarmScheduler.cancelDoseAlarmAndBackup`
- audit logs com `branch` + `source_scenario`

### Etapa 5 — Cleanup

- **#224 incluído** — `BootReceiver.java` margem 2h alarme atrasado (`LATE_ALARM_GRACE_MS = 2 * 60 * 60 * 1000L`) + flag `lateRecovery=true`
- **#223 incluído** — `src/hooks/usePushNotifications.js` deletado (deprecated re-export 7 linhas) + imports inline em `App.jsx` + `Settings/index.jsx`
- **#222 incluído** — `AlarmActivity.java` cleanup ~80 linhas código morto:
  - `mediaPlayer` + `vibrator` fields removidos
  - `startAlarmSound`, `startVibration`, `postPersistentNotification`, `cancelPersistentNotification`, `ensureChannel` funções removidas
  - `CHANNEL_ID doses_v2` + `NOTIF_ID_OFFSET` constants removidas
  - Imports limpos (NotificationChannel/NotificationManager/NotificationCompat/MediaPlayer/RingtoneManager/AudioAttributes/Vibrator/VibrationEffect/Uri/R)

## 📦 Commits release/v0.2.3.0

```
21f8f32 chore: abre release/v0.2.3.0 — bump vc 62→63
e45d1d5 feat(#215): Etapa 1 helper unificado scheduler 3 branches + canais novos
04bbbef feat(#215): Etapa 2 Cenário 01 — scheduler.js usa unifiedScheduler + janela dinâmica
a2eb69c feat(#215): Etapas 3+4+5 — backend cancel_alarms cross-device + Java helper paridade + cleanup
```

Pushed `origin/release/v0.2.3.0` (PR URL: https://github.com/lhenriquepda/medcontrol_v2/pull/new/release/v0.2.3.0).

## 🧪 Validações pendentes (user manual S25 Ultra)

13 cenários device detalhados em CHECKLIST §#215. Resumo:
- **01.A** alarme cheio caso normal + LocalNotification backup cancelada anti-duplicate
- **01.B** DnD janela → push tray vibração leve (canal dosy_tray_dnd)
- **01.C** criticalAlarm OFF → push tray normal (canal dosy_tray)
- **01.D** toggle ON→OFF Ajustes → cancelAlarmes + recadastra push
- **02.A** mark dose Tomada → alarme + push cancelados local + outros aparelhos <5s via FCM
- **02.B** Desfazer → alarme + push reagendados
- **02.C** cuidador compartilhado → ambos recebem alarme cheio em paralelo
- **02.D** cuidador com DnD próprio + dose paciente em janela → cuidador só push silencioso
- **03.A** force-stop + WorkManager 6h → device acorda + reagenda
- **03.B** cron 5am BRT → device recebe FCM data → DosyMessagingService agenda
- **03.C** Samsung One UI 7 sem battery whitelist → alarme nativo morto + LocalNotification backup dispara
- **Boot recovery** dose passou <2h durante boot → alarme imediato lateRecovery
- **Limite dinâmico** 200 doses/dia → horizon=24h registrado audit log

## 🎯 Próximos passos

1. **User:** Build AAB Android Studio + validação device S25 Ultra (13 cenários).
2. **Pós-validação:** tag git `v0.2.3.0` + merge master + Vercel deploy prod + Play Console upload Internal Testing.
3. Items #216 (limpar Edge notify-doses ref dose_alarms_scheduled), #218 (restaurar migrations locais), #219 (deletar Edges órfãs), #226 (device_id UUID padrão) ficam pra próximas releases.

## 📚 Referências

- Auditoria: [`contexto/auditoria/2026-05-13-alarme-push-auditoria.md`](../auditoria/2026-05-13-alarme-push-auditoria.md)
- CHECKLIST §#215 (refactor completo + 13 cenários validação) + §#220 #221 #222 #223 #224 #225 (items inclusos)
- ROADMAP §6.7 TURNAROUND P0 #215 + §6.6 BUGS + §6.5 MELHORIAS atualizados
- Edge deployed v18 (dose-trigger-handler) + v3 (daily-alarm-sync) via MCP
- Migration applied `expand_dose_change_notify_to_delete_v0_2_3_0`
