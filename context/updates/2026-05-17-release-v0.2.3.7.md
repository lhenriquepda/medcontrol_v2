# 2026-05-17 — Release v0.2.3.7 SHIPPED

**Tag:** `v0.2.3.7` · master commit `e0933f8`
**Play Console:** Internal Testing publicado 11:04 BRT · vc 70 vn 0.2.3.7
**Vercel prod:** auto-deploy via master push (dosymed.app v0.2.3.7 confirmado)

## Entregue (10 itens fechados)

### Performance bundle low-risk (auditoria device slow)
- **#272** F1 — App.jsx alarmWindow -30d/+60d → -1d/+14d (motivo original obsoletizado por #163)
- **#273** F3 — useDashboardPayload placeholderData via ref module-scope (elimina findAll por render)
- **#274** F6 — React.memo BottomNav + AppHeader
- **#275** F5 — persister throttleTime 1000ms → 5000ms

### Server-side caregiver flow (gaps fechados)
- **#279** P1 — Edge `dose-trigger-handler` v24 caregiver `notification` payload bypass Doze + `daily-alarm-sync` v5 inclui shares
- **#280** P1 — Edge `patient-share-handler` v4 + DB trigger ON `patient_shares` INSERT → push share notification
- **#281** P1 — Edge `dose-fire-time-notifier` v6 + pg_cron 1min + `doses.fire_notified_at` index parcial (idempotência) — cuidador app killed recebe fire-time tray

### Bug fixes P1
- **#282** AlarmScheduler idempotente (skip se `triggerAt+dosesHash` iguais) + DoseSyncWorker período 6h → 24h `REPLACE` policy backup Samsung Doze
- **#283** P1 BUG — RPCs `create_treatment_with_doses` + `register_sos_dose` derivam userId de `patient.userId` em vez de `auth.uid()` (cuidador criando dose pra paciente compartilhado agora gera `dose.userId=owner real`)

### Validação
- **#284** QA exaustivo re-validação completa 21/21 do zero — banco limpo + apps fresh-start ambos emuladores. Bloco A Owner (8/8) + Bloco B Cuidador (3/3) + Bloco C FCM/alarmes/cron (10/10). Relatório [`contexto/qa/QA_REPORT_v0_2_3_7_full_rerun.md`](../qa/QA_REPORT_v0_2_3_7_full_rerun.md). Scripts Appium reutilizáveis em `scripts/qa_*.mjs`.

## Migrations Supabase aplicadas

| Arquivo | Propósito |
|---|---|
| `20260516160000_patient_share_notification_trigger_v0_2_3_7.sql` | DB trigger ON patient_shares INSERT → pg_net Edge call |
| `20260516160500_dose_fire_notified_at_v0_2_3_7.sql` | Coluna `fire_notified_at` + index parcial idempotência |
| `20260516161000_dose_fire_time_cron_v0_2_3_7.sql` | pg_cron a cada 1 min → Edge `dose-fire-time-notifier` |
| `20260517060000_dose_change_notify_update_reschedule_v0_2_3_7.sql` | UPDATE pending→pending reagenda alarme |
| `20260517130000_rpc_use_patient_owner_userid_v0_2_3_7.sql` | RPCs derivam userId de patient.userId |

## Edge Functions deployadas

| Function | Versão |
|---|---|
| dose-trigger-handler | v24 |
| dose-fire-time-notifier | v6 |
| patient-share-handler | v4 |
| daily-alarm-sync | v5 |

## Cron jobs ativos

| jobid | schedule | descrição |
|---|---|---|
| 7 | `0 8 * * *` | daily-alarm-sync (5am BRT, diário) |
| 9 | `* * * * *` | dose-fire-time-notifier (1×/min) |

## Pendências device físico Samsung S25 Ultra (usuário)

- [ ] Toque real em notificação fire-time → abre DoseModal correto
- [ ] Alarme físico nas próximas 24h dispara som loop + AlarmActivity lockscreen
- [ ] Push de compartilhamento recebido em background/killed
- [ ] WorkManager dispara 1×/dia mesmo com Doze profundo Samsung Adaptive Battery

## Notas técnicas relevantes

1. **AAB CLI build:** `bundleRelease` em 1m48s, output 39.2 MB. `TEMP/TMP` redirect pra `C:\temp\gradle_tmp` resolve filter driver AF_UNIX em `AppData\Local\Temp`.
2. **Upload Play Console:** Chrome MCP via `§10` receita README — file_upload + form_input release notes + Salvar e publicar. Único aviso = símbolos depuração (não-bloqueador).
3. **Stale TanStack persist cache:** documentado como artefato após DB clean — não-bug, realtime resolve no próximo evento.
4. **register_sos_dose 2 overloads:** existe versão 5-arg e 6-arg (`p_force boolean`). Chamada precisa explicitar com 6 args ou casting de tipos.

## Counter ROADMAP

152 fechados / 80 abertos (release v0.2.3.7 fecha 10 itens: #272-#275 + #279-#284).
