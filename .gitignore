node_modules
dist
coverage
.env
.env.local
.env.*.local
.env.production
*.log
.DS_Store
Thumbs.db

.vercel

# Secrets / local notes (NEVER commit)
INFOS.md

# Claude Code worktrees
.claude/worktrees/

# Local Android SDK install (machine-specific, not project)
build-tools/
licenses/
platform-tools/
platforms/

# Supabase CLI temp + local binaries + DB dumps (may contain PII)
supabase/.temp/
tools/supabase.exe
tools/supabase.tar.gz
tools/*_dump.sql
tools/medcontrol_dump.sql

# Android release keystore — NEVER commit (loss = cannot update app)
*.keystore
*.jks
android/keystore.properties

# Built APKs distributed via Vercel public/ (12MB+ binaries, regenerated per release)
public/*.apk

# Android Studio release outputs (large binaries + metadata, regenerated per build)
android/app/release/
android/app/build/

# Capacitor-assets sources (committed but build artifacts excluded)
assets/icon-only.png

# Android Studio user-specific (.idea kept partial; ignore the volatile bits)
android/.idea/workspace.xml
android/.idea/usage.statistics.xml
android/.idea/shelf/
android/.idea/jarRepositories.xml
.keys-temp*

# Appium MCP QA (local dev)
screenshots/
.mcp.json
appium-capabilities.json
